#pragma once

#include "Math.h"

constexpr vec3 VEC_UP = vec3(0, 1, 0);
constexpr vec3 VEC_FORWARD = vec3(0, 0, 1);
constexpr vec3 VEC_RIGHT = vec3(1, 0, 0);

constexpr vec3 DEBUG_COLOR = vec3(1, 0, 1);  // Magenta
